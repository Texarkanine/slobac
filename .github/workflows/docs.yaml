name: Build and Deploy Docs Site

on:
  release:
    types:
      - published
  pull_request:
    # Build on all PRs so link-validation / strict-mode gates docs changes at review time.
    # Deploy job is skipped for PRs (see the `if:` on `deploy:` below).
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# One concurrent deploy to Pages; do not cancel in-progress prod deploys, but skip
# any runs queued between the in-progress one and the latest queued one.
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  taxonomy-index-drift:
    name: Taxonomy index drift-check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup uv
        uses: astral-sh/setup-uv@v8.0.0
        with:
          enable-cache: true

      - name: Install dev dependencies
        # --frozen: fail if uv.lock is out of date.
        run: uv sync --group dev --frozen

      - name: Regenerate taxonomy index
        run: uv run python scripts/gen_taxonomy_index.py

      - name: Verify no drift
        run: |
          if ! git diff --exit-code -- \
              skills/slobac-audit/SKILL.md \
              skills/slobac-audit/references/docs/taxonomy/README.md; then
            echo "::error::Taxonomy index is out of date."
            echo "Run 'uv run python scripts/gen_taxonomy_index.py' locally and commit the result."
            exit 1
          fi

      - name: Run unit tests
        run: uv run pytest

  build:
    name: Build site
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup uv
        uses: astral-sh/setup-uv@v8.0.0
        with:
          # Auto-keys the cache on pyproject.toml + uv.lock. uv also auto-provisions
          # Python per `requires-python` in pyproject.toml, so no separate
          # actions/setup-python step is needed.
          enable-cache: true

      - name: Install docs dependencies
        # --frozen: fail if uv.lock is out of date vs pyproject.toml. Lock drift
        # must be a PR-reviewable change, not a silent CI-time regeneration.
        # If this step fails: run `uv sync --group docs` locally and commit the
        # updated uv.lock.
        run: uv sync --group docs --frozen

      - name: Build site (strict)
        run: uv run properdocs build --strict

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: site

  deploy:
    name: Deploy to GitHub Pages
    # PR builds stop at the `build` job — they gate link validity without deploying.
    if: github.event_name != 'pull_request'
    needs: [build, taxonomy-index-drift]
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v5
