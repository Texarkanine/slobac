PASS (with one plan amendment applied)

Amendment: Step 4 file list extended to include `tests/fixtures/audit/README.md` (two stale taxonomy-path prose references on lines 44 & 48 surfaced by searching for the obsolete `skills/` + short directory name). Fixture directory itself (`tests/fixtures/audit/`) intentionally NOT renamed.

Cross-checks performed:
- TDD encoding: N/A for pure rename + docs sweep; per-step verification gates substitute (acceptable — `techContext.md` records "Testing Process: None yet").
- Convention compliance: `git mv` preserves history; `slobac-audit` directory name aligns with existing `name: slobac-audit` SKILL.md frontmatter and the prior `slobac-{scout,batch,cross-suite}` sibling-skill naming pattern (since folded into references/subagents/).
- Marketplace JSON files verified: `.cursor-plugin/plugin.json` uses `"skills": "./skills/"` glob; `.claude-plugin/plugin.json` has no skills field (auto-discovers). Neither references the directory name. No change needed — plan correctly skips them.
- Pre-rename orchestrator SKILL path verified: self-references use only relative `references/...` paths; rename-safe.
- Workflow files (`.github/workflows/`) verified: no obsolete absolute skill-path strings embedded.
- Sibling repo `txrk9-agent-plugins/README.md` line 16 confirmed as the only stale reference; marketplace JSONs do not reference the folder name.
