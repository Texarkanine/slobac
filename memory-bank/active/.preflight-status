# Preflight Status — Third Rework

**Result:** PASS with amendments

**Date:** 2026-04-29

## Findings

### PF1 (MAJOR, applied): `planning/VISION.md` has 14 `../docs/` references not covered by the plan.

VISION.md is a live planning document read on GitHub. After the move, all 14 `../docs/` relative links break (`../docs/principles.md`, `../docs/taxonomy/deliverable-fossils.md`, etc.). The plan has no step addressing this.

**Amendment:** Add step **T5d** to T5 (repo-root README step): update `planning/VISION.md` references from `../docs/` paths to published URLs (`https://texarkanine.github.io/slobac/...`). Published URLs are stable regardless of repo layout and are the correct target for a planning document read by external stakeholders.

### PF2 (advisory, not applied): `memory-bank/active/projectbrief.md` has 2 stale `../../docs/taxonomy/` references.

These will break after the move, but the project brief is an ephemeral file scoped to the current task and will be cleaned up during the archive phase. Not worth a plan step; note for awareness during archive.

### PF3 (advisory, not applied): `pymdownx.snippets` config is no longer load-bearing.

The `pymdownx.snippets` extension in `properdocs.yml` (`base_path: [.]`, `check_paths: true`) was the mechanism for the snippet-include wrappers. After the move, no files use `--8<--` directives. The config is harmless and may be useful for future content. No action needed.
