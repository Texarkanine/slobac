# Progress — Phase 1 Audit MVP

Ship Phase 1 of [`planning/VISION.md`](../../planning/VISION.md): an audit MVP for two smells (`deliverable-fossils` and `naming-lies`), packaged as harness-portable agent customizations runnable in Cursor and Claude Code. Creative-phase decisions required on (1) docs↔skill DRY mechanism and (2) customization granularity that survives Phase 2. Full details: [`projectbrief.md`](./projectbrief.md).

**Complexity:** Level 3

## History

- **Complexity analysis complete** — Level 3 determined; rationale in [`activeContext.md`](./activeContext.md). Next phase: Level 3 planning.
- **Plan (component analysis)** — components, cross-module dependencies, boundary changes, and invariants identified. `tasks.md` carries the full component analysis and invariant checklist. Two open questions flagged (OQ1 customization shape, OQ2 docs↔customization DRY); VISION §5 open questions #1 (output format → markdown), #3 (subset UX → natural language), #8 (artifact name → `slobac-audit.md`), and testing approach (fixture suites + manual verification) decided at plan-time with rationale in `tasks.md`.
- **Creative OQ1 — Customization primitive shape & granularity** — **Resolved (high confidence):** ur-Skill + per-smell entries under `references/smells/<slug>.md`, AgentSkills.io shape. See [`creative/creative-customization-shape.md`](./creative/creative-customization-shape.md). Resumes plan phase for OQ2.
- **Creative OQ2 — Docs ↔ customization DRY mechanism** — **Resolved (high confidence):** docs canonical; skill `references/smells/<slug>.md` carries audit-specific augmentation only; SKILL.md workflow instructs agent to read both files per in-scope smell. Structurally enforces the manifesto-independence invariant. `pymdownx.snippets`-based options eliminated structurally (build-time only; incompatible with agent-runtime or github.com). See [`creative/creative-docs-skill-dry.md`](./creative/creative-docs-skill-dry.md).
- **Plan phase complete** — TDD test plan, 14-step implementation plan, challenges, tech-validation note finalized in [`tasks.md`](./tasks.md). Next phase: Preflight.
- **Preflight PASS** — two implementation amendments applied inline (report default path; always-present augmentation file) + one advisory raised (report versioning). Status in [`.preflight-status`](./.preflight-status). Next phase transition (Preflight → Build) requires operator input per L3 workflow.
- **Build phase entered** — operator invoked `/niko-build`. Executing the 14-step implementation plan per [`tasks.md`](./tasks.md) in TDD order (fixtures + expected-findings first, then SKILL.md and augmentation files, then tech/docs updates).
- **Build PASS** — 12 plan steps executed, 2 skipped per plan (step 10 pre-marked unneeded; step 14 conditional pivot not triggered). Artefacts: skill at `skills/slobac-audit/` (SKILL.md + README + report-template + 2 per-smell augmentation files), fixtures at `tests/fixtures/audit/` (4 scenarios, each with Python file + expected-findings.md), root README section, techContext update. Integration checks: `properdocs build --strict` clean, fixtures parse as Python 3, all cross-links resolved after fixing 4 incorrect relative paths in fixture expected-findings files. Build details in [`activeContext.md`](./activeContext.md). Next phase: QA.
- **QA PASS** — one trivial fix applied (removed "Skill version" field from report template; scope-crept from preflight advisory that was explicitly marked "not applied"). No substantive issues. Three report-template sections (Summary, Tests considered but not flagged, Out-of-scope requests) kept as structural scaffolding consistent with plan intent and flagged for operator review in archive phase. Status in [`.qa-validation-status`](./.qa-validation-status). Next phase: Reflect.
- **Reflect COMPLETE** — full-lifecycle review written to [`reflection/reflection-phase-1-audit-mvp.md`](./reflection/reflection-phase-1-audit-mvp.md). Key insights: (1) prompt-artifact TDD (fixture-first) reshapes detection prose meaningfully, worth codifying for Phase 2; (2) OQ2's structural manifesto-independence made QA trivial; (3) "preflight advisory raised but not applied" is an ambiguous state that leaked scope during build. Persistent files reconciled: `systemPatterns.md` and `productContext.md` surgically updated. Next phase: Archive (requires operator input per L3 workflow).
- **Post-reflect rework initiated by operator** — operator identified that OQ2's creative decision is architecturally invalid: AgentSkills.io skills cannot reference files outside the skill's root directory. The in-repo relative refs (`skills/slobac-audit/references/**/*.md` → `../../../../docs/taxonomy/<slug>.md`) resolve correctly inside the SLOBAC working tree but break on any real install (`~/.claude/skills/slobac-audit/`, user-level Cursor install, marketplace distribution). Root cause: the creative analysis conflated filesystem co-location with runtime co-location. Entering Plan phase to re-scope OQ2 and the downstream implementation steps under the corrected runtime-root constraint. Reflection insight #2 ("OQ2 held up cleanly") is invalidated and will be revisited in the post-rework Reflect phase.
- **Plan (rework) — OQ2-redux opened, creative pending** — tasks.md amended with a Rework Context section documenting the defect and root cause; invariant #11 (Skill-root self-containment) codified; OQ2 marked invalidated; OQ2-redux opened with four candidate options (E: generator + drift-check CI gate; H: role-split operational playbook; K: vendored manual-sync; J: runtime fetch). Affected implementation steps annotated for re-authoring after creative closure; fixtures and report template preserved.
- **Creative (rework, OQ2-redux) COMPLETE** — architecture creative phase run under the corrected constraints (invariant #11 as a hard constraint; QA ranking preserved from original OQ2). Decision: Option E (generator + drift-check CI gate) with a two-directory file layout (`references/taxonomy/<slug>.md` generated; `references/smells/<slug>.md` hand-authored). Alternatives H, K, M, J each eliminated by traceable QA/constraint failure rather than judgment. Confidence is high with a calibration note: the prior OQ2 was also marked "high confidence" and missed a constraint, so this decision's confidence is explicitly conditional on the enumerated analysis axes being complete. Superseded OQ2 creative document preserved for traceability. See [`creative/creative-docs-skill-dry-redux.md`](./creative/creative-docs-skill-dry-redux.md).
- **Plan (rework) PASS** — rework implementation plan authored (R1–R11): generator script + CI drift-gate (R1–R3); SKILL.md workflow rewrite + augmentation-file preamble rewrite + README update (R4–R6); techContext + systemPatterns updates (R7–R8); operator-gate re-run + invariant #11 spot-check + reflection amendment (R9–R11). Two new behaviors added to the test plan (B8 drift-gate; B9 no-cross-root spot-check). Technology validation: one new stdlib-only Python script, one new CI job, no new runtime deps, `uv.lock` unchanged. Four rework-specific challenges enumerated with mitigations (authoring footgun, new CI failure mode, procedural-not-structural invariant #11 enforcement, calibration-debt from the prior OQ2). Status advanced to Preflight (post-rework).
- **Preflight (rework) PASS with amendments** — grep-based sweep of `skills/slobac-audit/**` surfaced a completeness gap: the plan's R4/R5/R6 enumerated only a subset of the cross-root references actually present. Six additional reference locations surfaced across SKILL.md (scope header, governor-rule cite), augmentation files (intra-manifesto cross-refs, principle cites), report-template.md (agent-facing citation instruction), and README.md (scope links, fixture links, obsolete failure-mode paragraph). Amendments applied inline: R4 split 4-way; R5 split 3-way; R5a added; R6 split 4-way; R1 and R10 clarified. Three advisories raised, not applied (PF3 scripts-dir convention; PF4 CI-job placement latitude; PF5 smells-manifest as Phase-2 entry-point candidate). Calibration-debt from the prior OQ2 carried forward to the build phase as a reminder that invariant-#11 claims must be grep-verified, not by-inspection. Status advanced to Build (post-rework) — requires operator input via `/niko-build`.
- **Build (rework) PASS** — all 11 authored rework steps executed (R1–R8, R10, R11; R9 is operator-gated). Generator `scripts/sync-taxonomy.py` authored stdlib-only (no `pyproject.toml` deps added), ran clean, produced 15 verbatim taxonomy copies under `skills/slobac-audit/references/taxonomy/` with "do not hand-edit" headers, and passes `--check` against the committed outputs. CI drift-check added as a step in the existing `docs.yaml` build job (PF4 advisory applied — co-located for `uv` env reuse; pre-merge gating via PR builds, unchanged). SKILL.md, both augmentation files, report-template.md, and the skill README were all rewritten to eliminate cross-root references: intra-skill content targets become `../taxonomy/<slug>.md` or `references/taxonomy/<slug>.md` paths; principle/governor-rule cites become published `https://texarkanine.github.io/slobac/` URLs; README manifesto/fixture links become absolute GitHub URLs. `techContext.md` and `systemPatterns.md` updated: the "canonical-docs-referenced-from-skill" pattern is replaced by the "generator-synchronised skill-bundle" pattern, and invariant #11 is codified as a system-level pattern so it is not forgotten on the next skill. Reflection insight #2 retracted with an explicit calibration note, plus companion retractions in the reflection's Summary and Cross-Phase Analysis sections. Final spot-check `rg '\]\(\.\./\.\./' skills/slobac-audit/` returns zero — no root-escaping links anywhere in the skill tree; the remaining `../principles.md#...` and `../glossary.md#...` links inside generated `references/taxonomy/*.md` are inert verbatim content per the R1 link-handling policy, explicitly excluded from invariant-#11 enforcement. R9 (operator-run harness validation) deferred per plan. Next phase: QA.
- **Post-build-2 rework initiated by operator** — operator rejected the shipped Option-E architecture on reading the artefacts. Two load-bearing objections: (1) committing generated content (the 15 `references/taxonomy/*.md` files produced by `scripts/sync-taxonomy.py`) is itself architecturally a smell; (2) the hand-authored `references/smells/*.md` augmentation files are ~35–60% restated manifesto content and silently drift when the manifesto is edited. Root cause (shared): OQ2-redux preserved the premise that `docs/taxonomy/*.md` is the authoring source of truth, which under invariant #11 forces copy-with-sync-discipline. The pattern itself is the problem. Operator's corrective direction: invert canonicality — skill bundle holds the canonical taxonomy, `docs/taxonomy/*.md` becomes `pymdownx.snippets`-include wrappers. Explicit new constraint surfaced: nobody reads `docs/*.md` raw — the rendered properdocs site is the reader surface (eliminating the original OQ2 objection that killed snippet-based options). QA and Reflect phases for the first rework are superseded.
- **Creative (second rework, OQ3) COMPLETE** — architecture creative phase run with the inversion premise and the "nobody reads raw" constraint as inputs. Decision: **Option γ — definitional canonical + discursive wrapper**. Canonical at `skills/slobac-audit/references/docs/taxonomy/<slug>.md` (15 files) carries every section of the smell's definition including a new first-class **False-positive guards** section (promoted from the augmentation files); `docs/taxonomy/<slug>.md` becomes usually-bare snippet-include wrappers. Augmentation files **deleted** — guards migrate to canonical, invocation phrases migrate to SKILL.md, restated manifesto content is dropped. Alternatives α (maximal canonical; functionally identical to γ but without named wrapper affordance for future reader-framing content) and β (minimal canonical; violates both "there might not be ANY wrapping" and "tight description" operator signals; degrades agent detection quality) each eliminated by traceable evaluation. Confidence is high with a recurring-calibration note: this is the third OQ2 pass, each prior "high confidence" met its stated constraints and missed a later-surfaced one; Phase-2 creative phases should add a "what will the operator's first reaction be?" checklist item before declaring high confidence. See [`creative/creative-canonical-entry-shape.md`](./creative/creative-canonical-entry-shape.md).
- **Plan (second rework) PASS** — second-rework implementation plan authored (S1–S14). S1 `git mv` migration of 15 manifesto entries to the canonical location; S2 thin snippet-include wrappers; S3 Phase-1 false-positive-guards promotion; S4 stub guards for non-Phase-1 entries; S5 SKILL.md workflow rewrite (single-file read) + inline invocation vocabulary; S6 delete `references/smells/*.md`; S7 delete generator + generated outputs + CI drift-check step; S8 report-template path update; S9 README rewrites; S10–S11 memory-bank `techContext.md` + `systemPatterns.md` updates (invariant #3 wording revision: manifesto-independence now structural; invariant #11 structurally enforced under the inversion; new "Canonical-in-bundle" pattern); S12 reflection second-retraction note; S13 operator-run harness validation; S14 `properdocs build --strict` gate. Test plan: B8 (drift-gate) removed; B10 (snippet-include rendering) and B11 (zero content duplication) added. Technology validation: `pymdownx.snippets` already enabled; no new runtime deps; generator + CI step removed (net one-job removal). Status advanced to Preflight (second rework) — requires operator input via `/niko-preflight` or equivalent.
- **Preflight (second rework) PASS with amendments** — two MAJOR findings fixed inline, three advisories raised. PF1 (MAJOR, applied): plan adds `## False-positive guards` as a new first-class section to all 15 canonical entries but omits updating `docs/taxonomy/README.md` (the taxonomy shape SoT); new step S4a added. PF2 (MAJOR, applied): S1 (`git mv`) and S2 (wrapper creation) as separate commits leave a broken intermediate state where `docs/taxonomy/<slug>.md` files don't exist; combined into a single commit. PF3 (advisory): augmentation files' emission-hints sections contain unique operational heuristics (RENAME-vs-STRENGTHEN tiebreaker, Phase-A-default guidance) being dropped; recoverable during harness validation if detection quality degrades. PF4 (advisory): properdocs "Edit this page" links point to wrappers post-migration; consistent with "nobody reads raw" premise. PF5 (advisory): verify `pymdownx.snippets` resolves paths outside `docs_dir` during S14. Convention compliance, dependency impact, and conflict detection all clear after amendments. Status in [`.preflight-status`](./.preflight-status). Next phase: Build (second rework) — requires operator input via `/niko-build`.
- **Build (second rework) PASS** — all implementation steps executed. S1+S2 combined commit: `git mv` 15 entries to `skills/slobac-audit/references/docs/taxonomy/` + 15 snippet-include wrappers at `docs/taxonomy/`. S3: Phase-1 false-positive guards promoted (2 guards from deliverable-fossils, 4 from naming-lies). S4: stub guards added to 13 non-Phase-1 entries. S4a: README shape spec updated. S5: SKILL.md rewritten (single-file canonical read, inline invocation vocabulary, published-URL governor-rule cite). S6: augmentation files + `references/smells/` deleted. S7: no-op (first-rework generator artifacts were never committed). S8: report-template citation path updated. S9: README layout, self-contained-bundle paragraph, fixture links as GitHub URLs, docs-tree caveat removed. S10–S11: memory-bank techContext + systemPatterns updated (canonical-in-bundle pattern, invariant #11 structural enforcement, authoring-model subsection). S12: reflection second retraction. S13: operator-run harness validation deferred per plan. S14: `properdocs build --strict` passes clean. B9/B10/B11 verification gates pass. No deviations from plan. Next phase: QA.
- **QA (second rework) PASS** — one trivial fix applied (stale "augmentation file" reference in SKILL.md Constraints section → "canonical entry"). No substantive issues; no YAGNI/DRY/KISS violations; all verification gates confirmed. Status in [`.qa-validation-status`](./.qa-validation-status). Next phase: Reflect.
- **Reflect (second rework) PASS** — full lifecycle review appended to the existing reflection document as a "Second Rework Addendum." Requirements vs Outcome: all met, no gaps. Plan accuracy: S1–S14 correct as preflight-amended. Creative phase: OQ3 held up; calibration discipline noted. Build/QA: one trivial fix, shell connectivity glitch (phantom commit, no data loss). Persistent file reconciliation: `productContext.md` fixed (stale `references/smells/` path); `systemPatterns.md` and `techContext.md` clean. Key technical insight: snippet-include inverts authoring surface without losing reader surface — zero-maintenance sync. Key process insight: when multiple creative passes fail at the same boundary, the constraint may be wrong — escalate before attempting. Next phase: Archive.
- **Third rework initiated by operator** — operator review of the shipped second-rework artefacts during pre-merge prep identified two remaining architectural seams: (1) SKILL.md Constraints section still uses pre-inversion "manifesto is canonical / audit cites the manifesto" language; (2) the snippet-include wrappers at `docs/taxonomy/*.md` are pure indirection — 15 files that exist solely to bridge properdocs's `docs_dir: docs` expectation with the canonical content living inside the skill. Operator's corrective direction: move the **entire** `docs/` tree into `skills/slobac-audit/references/docs/` and point `properdocs.yml` `docs_dir` at it. Eliminates the snippet-include indirection, puts the full manifesto inside the skill root (satisfying invariant #11 for all manifesto content, not just taxonomy), and makes `principles.md`/`glossary.md` directly referenceable at agent-runtime. Entering Plan phase.
- **Plan (third rework) PASS** — 10-step implementation plan authored (T1–T10): atomic move of remaining docs files + wrapper deletion + config update (T1); SKILL.md Constraints fix + governor-rule cite update (T2); fixture path references (T3); skill README (T4); repo-root README (T5); techContext + systemPatterns (T6–T7); reflection note (T8); verification gates (T9–T10). No open questions, no creative phase, no new dependencies. Test plan: B10 updated (docs build under new `docs_dir`), B11 removed (no wrappers to check), B13 added (relative links resolve at filesystem location). Status advanced to Preflight.
- **Preflight (third rework) PASS with amendments** — one MAJOR finding (PF1): `planning/VISION.md` has 14 `../docs/` references not covered by the plan; added T5d to update them to published URLs. Two advisories not applied: PF2 (`projectbrief.md` stale refs — ephemeral file, archive cleanup), PF3 (`pymdownx.snippets` config no longer load-bearing — harmless, keep). Status in `.preflight-status`. Next phase: Build — requires operator input via `/niko-build`.
- **Build (third rework) PASS** — all 10 implementation steps executed (T1–T10). T1: atomic `git mv` of 6 remaining docs files + deletion of 15 snippet-include wrappers + `properdocs.yml` config update (single commit). T2: SKILL.md Constraints rewritten + governor-rule cite changed to intra-skill path. T3: fixture expected-findings `docs/` refs → published URLs + stale `references/smells/` refs fixed. T4: skill README layout expanded + canonical paragraph broadened. T5: repo-root README + T5d (14 VISION.md refs → published URLs). T6–T7: techContext + systemPatterns rewritten for full-manifesto-in-bundle pattern. T8: reflection evolution note. T9: `properdocs build --strict` passes clean. T10: invariant #11 spot-check passes (zero cross-root escapes). No deviations from plan. Next phase: QA.
- **QA (third rework) PASS** — two trivial fixes applied: stale `docs/` heading in systemPatterns.md → "manifesto tree"; stale `docs/` ref in productContext.md → current path. No substantive issues, no YAGNI/DRY/KISS violations. All verification gates confirmed. Status in `.qa-validation-status`. Next phase: Reflect.
- **Reflect (third rework) COMPLETE** — full third-rework addendum appended to the reflection document. Key insight: the four-pass architecture journey (OQ2 → OQ2-redux → OQ3 → third rework) converged on the simplest possible structure — direct `docs_dir` pointer, no sync mechanism, no wrappers. Each intermediate was necessary to understand why it was unnecessary. Persistent files already clean from build/QA; no reconciliation changes needed. Next phase: Archive (requires operator input via `/niko-archive`).
