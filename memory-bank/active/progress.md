# Progress

## Summary

Rename the single registered SLOBAC skill directory from `skills/audit/` to `skills/slobac-audit/` so that the token displayed/invoked in both Cursor (`/slobac-audit`) and Claude Code (display `slobac-audit`, invocation `/slobac:slobac-audit`) reads the same. Precede the rename with a brief investigation to rule out any native parity mechanism in either harness.

**Complexity:** Level 2

## Log

- Complexity analysis complete; Level 2 determined.
