# Onboarding

Follow these steps when bringing up a new environment.

1. First, install the CLI from the release tarball.
2. Next, run the smoke check against a scratch workspace.
3. Finally, report failures upstream with logs attached.
